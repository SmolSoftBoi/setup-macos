## 1. Update README source references

- [x] 1.1 Replace the README one-liner under the title with the canonical repository URL `https://github.com/SmolSoftBoi/setup-macos.git`.
- [x] 1.2 Update the installation command snippet to clone `https://github.com/SmolSoftBoi/setup-macos.git` into `~/.setup-macos` and execute `~/.setup-macos/setup`.

## 2. Validate onboarding guidance

- [x] 2.1 Review `README.md` to confirm the one-liner and install command are consistent with each other and the repository `origin` URL.
- [x] 2.2 Confirm no unrelated README sections were changed and the rendered Markdown remains clear.
